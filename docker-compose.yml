services:
  notes-mcp-server:
    build: .
    container_name: notes-mcp-server
    user: "568:568"
    ports:
      - "8083:8000"
    volumes:
      - /home/peter/syncthing/obsidian/Homelab:/app/vault:rw
    environment:
      - VAULT_PATH=${VAULT_PATH:-/app/vault}
      - HOST=${HOST:-0.0.0.0}
      - PORT=${PORT:-8000}
    restart: unless-stopped


