# mcp-servers

This is a collection of custom MCPs I use, built from scratch.

Each MCP lives on its own branch (for example `metrics_mcp`, `notes_mcp`). Check out the branch for the server you care about.
